# ===================== APPLICATION =====================
spring.application.name=studentRaytings

# ===================== DATABASE =====================
spring.datasource.url=jdbc:postgresql://localhost:5432/StudentRayting
spring.datasource.username=postgres
spring.datasource.password=12345
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================== JPA / HIBERNATE =====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# B�T�N S�TUNLARI (user_type, group_number, exam_score v? s.) AVTOMAT?K YARADACAQ!!!
spring.jpa.hibernate.use-new-id-generator-mappings=false

# PostgreSQL-d? b�y�k/ki�ik h?rf problemi olmas?n dey?
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# ?lk d?f? data.sql i?l?sin dey? (?g?r istifad? edirs?ns?)
spring.jpa.defer-datasource-initialization=true

# ===================== SERVER =====================
server.port=8080

# ===================== LOGGING (ist?y? g�r?) =====================
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.type.descriptor.sql=trace

# Control automatic insertion of sample quiz questions by QuizDataInitializer
# Set to true to enable (for local testing), false to disable.
quiz.data.initializer.enabled=false

