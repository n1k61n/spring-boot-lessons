package com.example.emin.repositories.languages;

import com.example.emin.models.About;
import com.example.emin.models.languages.CareerLanguage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CareerLanguageRepository extends JpaRepository<CareerLanguage, Long> {
}
