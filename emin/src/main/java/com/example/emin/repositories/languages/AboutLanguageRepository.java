package com.example.emin.repositories.languages;

import com.example.emin.models.About;
import com.example.emin.models.languages.AboutLanguage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AboutLanguageRepository extends JpaRepository<AboutLanguage, Long> {
}
